This folder contains sample data used by the sample*.html files and can be deleted.

NOTE: We don't put the sample*.html files in this folder to avoid paths like 
'../js/Atm.js' and use 'js/Atm.js' that can be more useful if we use those 
'sample*.html' as base files.